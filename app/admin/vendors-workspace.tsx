"use client";

import { useEffect, useState } from "react";

type Vendor = { id: string; business_name: string; owner_name: string; login_email: string; status: string; store_visibility: string; featured: boolean; slug: string; commission_rate: number; automatic_approval: boolean };
const empty = { businessName: "", ownerName: "", loginEmail: "", initialPassword: "", confirmPassword: "", phone: "", whatsappNumber: "", logoUrl: "", coverUrl: "", description: "", address: "", city: "", state: "", pinCode: "", gstNumber: "", panNumber: "", accountHolderName: "", accountNumber: "", bankName: "", branchName: "", ifscCode: "", upiId: "", commissionPercentage: "0", status: "Active", storeVisibility: "Hidden", featured: false, automaticApproval: false };

export default function AdminVendorsWorkspace({ onNotify }: { onNotify: (message: string) => void }) {
  const [tab, setTab] = useState("All Vendors"); const [vendors, setVendors] = useState<Vendor[]>([]); const [form, setForm] = useState(empty); const [selected, setSelected] = useState<Vendor | null>(null); const [password, setPassword] = useState(""); const [error, setError] = useState(""); const [isCreating, setIsCreating] = useState(false);
  const load = () => fetch("/api/admin/vendors", { cache: "no-store" }).then(async (response) => { const body = await response.json() as { vendors?: Vendor[]; error?: string }; if (!response.ok) throw new Error(body.error || "Could not load vendors"); setVendors(body.vendors || []); }).catch((caught) => setError(caught instanceof Error ? caught.message : "Could not load vendors"));
  useEffect(() => { void load(); }, []);
  const create = async (event: React.FormEvent) => { event.preventDefault(); if (isCreating) return; setError(""); if (form.initialPassword !== form.confirmPassword) return setError("Passwords do not match."); setIsCreating(true); try { const response = await fetch("/api/vendors", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ ...form, commissionPercentage: Number(form.commissionPercentage) }) }); const body = await response.json().catch(() => ({})) as { error?: string }; if (!response.ok) return setError(body.error || `Could not create vendor (${response.status}).`); setForm(empty); setTab("All Vendors"); onNotify("Vendor created"); void load(); } catch { setError("Could not reach the vendor service. Make sure the local server is running and try again."); } finally { setIsCreating(false); } };
  const update = async (id: string, body: Record<string, unknown>, message: string) => { const response = await fetch(`/api/admin/vendors/${id}`, { method: "PATCH", headers: { "content-type": "application/json" }, body: JSON.stringify(body) }); const result = await response.json() as { error?: string }; if (!response.ok) return setError(result.error || "Could not update vendor"); onNotify(message); void load(); };
  const field = (key: keyof typeof form, label: string, type = "text", className = "") => <label className={className}>{label}<input type={type} value={String(form[key])} onChange={(event) => setForm({ ...form, [key]: type === "checkbox" ? event.target.checked : event.target.value })} /></label>;
  return <section className="panel module-workspace vendors-workspace"><div className="module-workspace-head"><div><p className="eyebrow">MARKETPLACE</p><h2>Vendors</h2><p>Manage vendor accounts, approvals, orders, commissions, payouts, and reports without changing the existing store workspace.</p></div><div className="module-actions"><button className="module-primary" onClick={() => setTab("Create Vendor")}>+ Create vendor</button></div></div><div className="vendor-admin-tabs">{["All Vendors", "Create Vendor", "Vendor Products", "Vendor Orders", "Commissions", "Payouts", "Vendor Reports", "Vendor Settings"].map((item) => <button className={tab === item ? "active" : ""} key={item} onClick={() => setTab(item)}>{item}</button>)}</div>{error && <p className="vendor-error" role="alert">{error}</p>}{tab === "Create Vendor" && <form className="vendor-form-card admin-vendor-form" onSubmit={create}><div className="vendor-form-heading"><div><p className="eyebrow">NEW ACCOUNT</p><h3>Create vendor</h3></div><p>Set up the vendor profile, login access, payout details, and storefront controls.</p></div><div className="vendor-form-section"><div className="vendor-form-section-heading"><h4>Account access</h4><p>These details are used by the vendor to sign in.</p></div><div className="vendor-form-grid vendor-form-grid-three">{field("businessName", "Vendor / business name")} {field("ownerName", "Owner name")} {field("loginEmail", "Login email", "email")} {field("initialPassword", "Initial password", "password")} {field("confirmPassword", "Confirm password", "password")}</div></div><div className="vendor-form-section"><div className="vendor-form-section-heading"><h4>Contact & branding</h4><p>Add the public contact information and storefront imagery.</p></div><div className="vendor-form-grid vendor-form-grid-three">{field("phone", "Phone number")} {field("whatsappNumber", "WhatsApp number")} {field("logoUrl", "Logo image URL")} {field("coverUrl", "Cover image URL", "text", "vendor-form-wide")}</div></div><div className="vendor-form-section"><div className="vendor-form-section-heading"><h4>Business details</h4><p>Help customers and operations teams identify the business.</p></div><div className="vendor-form-grid vendor-form-grid-three"><label className="vendor-form-wide">Business address<textarea value={form.address} onChange={(event) => setForm({ ...form, address: event.target.value })} placeholder="Street address, building, or landmark" /></label>{field("city", "City")} {field("state", "State")} {field("pinCode", "PIN code")}<label className="vendor-form-wide">Business description<textarea value={form.description} onChange={(event) => setForm({ ...form, description: event.target.value })} placeholder="A short description of this vendor" /></label></div></div><div className="vendor-form-section"><div className="vendor-form-section-heading"><h4>Tax & payouts</h4><p>Keep compliance and settlement information together.</p></div><div className="vendor-form-grid vendor-form-grid-three">{field("gstNumber", "GST number")} {field("panNumber", "PAN number")} {field("accountHolderName", "Bank account holder")} {field("accountNumber", "Bank account number")} {field("bankName", "Bank name")} {field("branchName", "Branch name")} {field("ifscCode", "IFSC code")} {field("upiId", "UPI ID")}</div></div><div className="vendor-form-section"><div className="vendor-form-section-heading"><h4>Store controls</h4><p>Choose how this vendor appears and how products are handled.</p></div><div className="vendor-form-grid vendor-form-grid-three">{field("commissionPercentage", "Commission percentage", "number")}<label>Account status<select value={form.status} onChange={(event) => setForm({ ...form, status: event.target.value })}><option>Active</option><option>Suspended</option><option>Inactive</option></select></label><label>Store visibility<select value={form.storeVisibility} onChange={(event) => setForm({ ...form, storeVisibility: event.target.value })}><option>Visible</option><option>Hidden</option></select></label></div><div className="vendor-form-options"><label><input type="checkbox" checked={form.featured} onChange={(event) => setForm({ ...form, featured: event.target.checked })} /><span><strong>Featured vendor</strong><small>Highlight this vendor in marketplace placements.</small></span></label><label><input type="checkbox" checked={form.automaticApproval} onChange={(event) => setForm({ ...form, automaticApproval: event.target.checked })} /><span><strong>Automatic product approval</strong><small>Publish new vendor products without manual review.</small></span></label></div></div><div className="vendor-form-actions"><p>Fields can be updated later from the vendor account manager.</p><span className="vendor-form-action-stack">{error && <small className="vendor-form-error" role="alert">{error}</small>}<button className="module-primary" type="submit" disabled={isCreating}>{isCreating ? "Creating vendor…" : "Create vendor securely"}</button></span></div></form>}{tab === "All Vendors" && <div className="vendor-admin-list">{vendors.map((vendor) => <article className="vendor-admin-row" key={vendor.id}><div><strong>{vendor.business_name}</strong><small>{vendor.owner_name} · {vendor.login_email} · /vendors/{vendor.slug}</small></div><span>{vendor.status} · {vendor.store_visibility}</span><div className="module-actions"><button className="module-secondary" onClick={() => setSelected(vendor)}>Manage</button><button className="module-secondary" onClick={() => void update(vendor.id, { action: "force-logout" }, "Vendor sessions invalidated")}>Force logout</button></div></article>)}{!vendors.length && <p className="muted">No vendors created yet.</p>}</div>}{tab !== "All Vendors" && tab !== "Create Vendor" && <div className="vendor-admin-placeholder"><h3>{tab}</h3><p>This workspace is connected to the vendor data layer. Choose a vendor from All Vendors to manage account status, visibility, password sessions, and product approvals.</p><button className="module-secondary" onClick={() => setTab("All Vendors")}>View vendors</button></div>}{selected && <div className="product-modal-backdrop" onClick={() => setSelected(null)}><div className="product-modal-card" onClick={(event) => event.stopPropagation()}><button className="product-modal-close" onClick={() => setSelected(null)}>×</button><p className="eyebrow">VENDOR ACCOUNT</p><h3>{selected.business_name}</h3><label>Status<select value={selected.status} onChange={(event) => void update(selected.id, { status: event.target.value }, "Vendor status updated")}><option>Active</option><option>Suspended</option><option>Inactive</option></select></label><label>Store visibility<select value={selected.store_visibility} onChange={(event) => void update(selected.id, { storeVisibility: event.target.value }, "Vendor visibility updated")}><option>Visible</option><option>Hidden</option></select></label><label>New password<input type="password" value={password} onChange={(event) => setPassword(event.target.value)} placeholder="Set a new password" /></label><button className="module-primary" onClick={() => { void update(selected.id, { action: "reset-password", password }, "Vendor password changed and sessions invalidated"); setPassword(""); }}>Change password</button></div></div>}</section>;
}
